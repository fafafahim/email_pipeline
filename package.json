{
  "name": "email_pipeline",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp -r frontend dist && cp -r export dist && cp -r output dist && echo '<h1>Hello Netlify</h1>' > dist/index.html"
  },
  "description": "Frontend for email codebase cleaning",
  "main": "azure-speech.js",
  "dependencies": {
    "dotenv": "^16.0.0",
    "microsoft-cognitiveservices-speech-sdk": "^1.22.1"
  }
}
